# 学习笔记

## Chunk 不是越小越好

过小会丢失上下文，过大则混入无关信息并增加 Token。`chunk_overlap` 用于保留段落边界附近的信息，但过大又会制造重复结果。先用真实问题评测 Recall，再调大小。

## 引用为何使用 Chunk ID

Prompt 给每段证据一个稳定 ID，模型只允许返回这些 ID。Pipeline 会过滤未知引用。生产系统还应把 Chunk ID 映射回文档版本、页码和权限。

## 检索分数不是事实置信度

余弦分数表示向量接近，不表示内容正确。`min_score` 只能过滤明显不相关结果，不能替代来源质量控制和答案评测。
