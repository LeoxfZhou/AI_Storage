# 学习笔记

## Prompt 和 Schema 分工

Prompt 解释字段的业务语义，Schema 约束字段类型、枚举和长度。只写 Prompt 会得到“看起来像 JSON”的文本；只写 Schema 又可能让模型不知道 `urgent` 的业务定义。

## 为什么枚举比自由字符串好

下游数据库、路由和统计需要有限集合。`priority: "有点急"` 对人能读，对程序却不稳定；枚举会让错误在边界立刻暴露。
