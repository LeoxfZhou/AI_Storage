# 学习笔记

## 为什么不能 `globals()[tool_name]`

模型生成的工具名是不可信输入。白名单 Registry 只暴露审核过的函数，避免任意函数或内部管理接口被调用。

## 工具错误为什么返回模型

参数错误可能通过补充信息恢复。模板把错误序列化为 `function_call_output`，模型可向用户追问；但达到轮数上限后必须停止，防止无限循环和费用失控。

## 审批不等于一句 Prompt

“执行前请询问用户”只是行为提示。真正安全边界是应用侧 `approved_side_effects`，即使模型跳过询问也无法创建工单。
