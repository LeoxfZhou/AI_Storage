# `<project_name>`

## 1. 项目目标

- 解决的问题：`<一句话说明>`
- 目标用户：`<用户角色>`
- 成功标准：`<可以量化的验收条件>`
- 不适用场景：`<明确边界>`

## 2. 输入与输出

说明输入来源、字段、大小限制、隐私等级，以及正常输出和错误输出。

## 3. 执行流程

按真实顺序描述：入口 -> 校验 -> 检索/工具 -> 模型 -> 后处理 -> 输出。

## 4. 模型清单

| 角色 | Provider / Model | 选择原因 | 替换方案 |
|---|---|---|---|
| generation | `<provider/model>` | `<原因>` | `<候选>` |

每个模型必须在 `models/<role>/README.md` 中维护完整模型卡。

## 5. 运行、测试与评测

给出从空环境开始可复制执行的命令，并区分离线测试与真实 API 集成测试。

## 6. 配置与调参

列出配置文件、环境变量、预设及覆盖顺序，不在文档中放真实密钥。

## 7. 限制、风险与排错

至少记录数据不足、模型不支持参数、限流、超时、幻觉和隐私风险。
